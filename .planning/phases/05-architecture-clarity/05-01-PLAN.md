---
phase: 05-architecture-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/quantum_substrate/protocols.py
  - src/quantum_substrate/coherence.py
  - src/quantum_substrate/surprise.py
  - src/quantum_substrate/tunneling.py
  - src/quantum_substrate/__init__.py
autonomous: true

must_haves:
  truths:
    - "Substrate can be imported without any agentic layer imports"
    - "No TYPE_CHECKING imports from agentic in substrate modules"
    - "Existing substrate tests continue to pass"
  artifacts:
    - path: "src/quantum_substrate/protocols.py"
      provides: "SubstratePattern protocol definition"
      contains: "class SubstratePattern"
    - path: "src/quantum_substrate/coherence.py"
      provides: "Coherence using protocol types"
      contains: "SubstratePattern"
    - path: "src/quantum_substrate/surprise.py"
      provides: "Surprise using protocol types"
      contains: "SubstratePattern"
    - path: "src/quantum_substrate/tunneling.py"
      provides: "Tunneling using protocol types"
      contains: "SubstratePattern"
  key_links:
    - from: "src/quantum_substrate/coherence.py"
      to: "src/quantum_substrate/protocols.py"
      via: "import SubstratePattern"
      pattern: "from.*protocols.*import.*SubstratePattern"
    - from: "src/agentic/evolving_pattern.py"
      to: "SubstratePattern protocol"
      via: "structural subtyping (implements protocol attributes)"
      pattern: "bits.*original_bits.*phases.*coherence"
---

<objective>
Create SubstratePattern protocol and refactor substrate modules to eliminate agentic dependencies.

Purpose: Achieves ARCH-01 (clear separation) and ARCH-02 (LLM-independence) by inverting the dependency direction. Substrate defines what it needs via protocol; agentic layer implements it.

Output: `protocols.py` with SubstratePattern protocol; coherence.py, surprise.py, tunneling.py updated to use protocol instead of TYPE_CHECKING imports from agentic.
</objective>

<execution_context>
@C:\Users\jinwi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jinwi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-architecture-clarity/05-RESEARCH.md
@src/quantum_substrate/coherence.py
@src/quantum_substrate/surprise.py
@src/quantum_substrate/tunneling.py
@src/agentic/evolving_pattern.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SubstratePattern protocol</name>
  <files>src/quantum_substrate/protocols.py</files>
  <action>
Create `src/quantum_substrate/protocols.py` with a `SubstratePattern` protocol that defines the interface substrate modules need.

The protocol must include:
- `dim: int` - pattern dimensionality
- `bits: Set[int]` property - current active bit indices
- `original_bits: FrozenSet[int]` property - frozen original bits
- `phases: Dict[int, float]` property - phase at each active bit
- `coherence: float` property (read/write) - current coherence 0-1
- `last_access_tick: int` property (read/write) - global tick of last access
- `embeddedness: float` property (read-only) - structural embeddedness
- `stability_score: float` property (read-only) - stability for crystallization
- `record_access() -> None` method - record access without modification

Use `@runtime_checkable` decorator so `isinstance()` checks work.

Include docstring explaining:
1. This is the interface for patterns usable by substrate operations
2. Any class implementing these attributes works with coherence/surprise/tunneling
3. EvolvingPattern in agentic layer implements this via structural subtyping
  </action>
  <verify>
```bash
python -c "from quantum_substrate.protocols import SubstratePattern; print('Protocol loaded')"
```
  </verify>
  <done>SubstratePattern protocol exists with all required attributes; can be imported from quantum_substrate.protocols</done>
</task>

<task type="auto">
  <name>Task 2: Refactor substrate modules to use protocol</name>
  <files>
    src/quantum_substrate/coherence.py
    src/quantum_substrate/surprise.py
    src/quantum_substrate/tunneling.py
    src/quantum_substrate/__init__.py
  </files>
  <action>
Update the three substrate modules that currently have TYPE_CHECKING imports from agentic:

**coherence.py:**
1. Remove `if TYPE_CHECKING: from agentic.evolving_pattern import EvolvingPattern`
2. Add `from quantum_substrate.protocols import SubstratePattern`
3. Change all type hints from `"EvolvingPattern"` to `SubstratePattern`
4. Keep the SurpriseResult import under TYPE_CHECKING (that's within substrate)

**surprise.py:**
1. Remove `if TYPE_CHECKING: from agentic.evolving_pattern import EvolvingPattern`
2. Add `from quantum_substrate.protocols import SubstratePattern`
3. Change all type hints from `"EvolvingPattern"` to `SubstratePattern`

**tunneling.py:**
1. Remove `if TYPE_CHECKING: from agentic.evolving_pattern import EvolvingPattern`
2. Add `from quantum_substrate.protocols import SubstratePattern`
3. Change all type hints from `"EvolvingPattern"` to `SubstratePattern`

**__init__.py:**
1. Add `from quantum_substrate.protocols import SubstratePattern` to imports
2. Add `"SubstratePattern"` to `__all__` list

This makes substrate completely independent of agentic layer at both import-time AND runtime.
  </action>
  <verify>
```bash
# Verify no agentic imports in substrate
grep -r "from agentic" src/quantum_substrate/ && echo "FAIL: agentic imports found" || echo "PASS: no agentic imports"

# Verify imports work
python -c "from quantum_substrate import SubstratePattern, CoherenceManager, SurpriseDetector, attempt_tunneling; print('All imports successful')"

# Run existing tests
pytest tests/quantum_substrate/ -v --tb=short
```
  </verify>
  <done>
- No `from agentic` imports in any substrate module
- SubstratePattern exported from quantum_substrate
- All existing substrate tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify EvolvingPattern satisfies protocol</name>
  <files>tests/quantum_substrate/test_protocol_compliance.py</files>
  <action>
Create a test file that verifies EvolvingPattern structurally satisfies SubstratePattern protocol.

Test file: `tests/quantum_substrate/test_protocol_compliance.py`

Contents:
1. Import SubstratePattern from quantum_substrate.protocols
2. Import EvolvingPattern from agentic.evolving_pattern
3. Test `isinstance(EvolvingPattern.from_text("test"), SubstratePattern)` returns True
4. Test that all protocol attributes are accessible on EvolvingPattern instance
5. Test that coherence get/set works through protocol interface
6. Test that substrate operations (CoherenceManager, SurpriseDetector) work with EvolvingPattern

This proves ARCH-03: agentic layer builds on substrate through documented interfaces.
  </action>
  <verify>
```bash
pytest tests/quantum_substrate/test_protocol_compliance.py -v
```
  </verify>
  <done>
- test_protocol_compliance.py exists
- EvolvingPattern passes isinstance check against SubstratePattern
- All protocol attributes accessible
- Substrate operations work with EvolvingPattern instances
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Import independence:** `python -c "import quantum_substrate"` succeeds without any agentic imports
2. **No circular imports:** Both `import quantum_substrate` and `import agentic` work
3. **Existing tests pass:** `pytest tests/quantum_substrate/ tests/agentic/ -v` all pass
4. **Protocol compliance:** EvolvingPattern works with all substrate operations
</verification>

<success_criteria>
- [ ] protocols.py exists with SubstratePattern protocol
- [ ] coherence.py uses SubstratePattern, no agentic imports
- [ ] surprise.py uses SubstratePattern, no agentic imports
- [ ] tunneling.py uses SubstratePattern, no agentic imports
- [ ] SubstratePattern exported from quantum_substrate
- [ ] EvolvingPattern satisfies SubstratePattern protocol (test proves it)
- [ ] All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-architecture-clarity/05-01-SUMMARY.md`
</output>
