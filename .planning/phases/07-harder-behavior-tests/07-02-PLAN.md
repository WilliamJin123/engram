---
phase: 07-harder-behavior-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/stress/test_interference.py
autonomous: true

must_haves:
  truths:
    - "Interference retrieval finds target patterns among semantic near-misses and random noise"
    - "All three success criteria are empirically tested: strict (top-1), relaxed (top-K), relative (above near-misses)"
    - "Near-miss count parametrization reveals degradation behavior"
    - "Both precision (near-misses rejected) and recall (targets found) are validated"
  artifacts:
    - path: "tests/stress/test_interference.py"
      provides: "TEST-02 interference retrieval stress tests"
      contains: "class TestInterferenceRetrieval"
  key_links:
    - from: "tests/stress/test_interference.py"
      to: "tests/conftest.py"
      via: "noisy_memory fixture and NoiseConfig"
      pattern: "noisy_memory|NoiseConfig"
    - from: "tests/stress/test_interference.py"
      to: "agentic.memory_store"
      via: "retrieve() with interference method"
      pattern: 'retrieve.*method.*interference'
---

<objective>
Create TEST-02 interference retrieval stress tests with noisy memory

Purpose: Validate that interference retrieval finds target patterns among semantic near-misses and random noise, testing all three success criteria (top-1, top-K, relative ranking) and observing degradation with increasing near-miss counts.

Output: tests/stress/test_interference.py with comprehensive TEST-02 coverage
</objective>

<execution_context>
@C:\Users\jinwi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\jinwi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-harder-behavior-tests/07-CONTEXT.md
@.planning/phases/07-harder-behavior-tests/07-RESEARCH.md
@tests/conftest.py
@src/agentic/memory_store.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TEST-02 interference retrieval tests</name>
  <files>tests/stress/test_interference.py</files>
  <action>
Create tests/stress/test_interference.py implementing TEST-02 (interference retrieval with noisy memory):

```python
"""TEST-02: Interference retrieval tests with noisy memory.

Validates that interference retrieval finds target patterns among
semantic near-misses and random clutter.

Tests all three success criteria:
- Strict: Target is top-1 result
- Relaxed: Target in top-K results
- Relative: Target ranked above all near-misses

All tests output metrics for Phase 8 degradation curve analysis.
"""
```

Import from tests.stress.conftest: STRESS_NOISE_LEVELS, StressMetrics, capture_retrieval_metrics, evaluate_success_criteria

Import from tests.conftest: NoiseLevel, NoiseConfig, noisy_memory

Implement TestInterferenceRetrieval class with:

1. **test_target_retrieval_basic** (parametrized by noise_level):
   - Create noisy memory with single target "The quick brown fox jumps over the lazy dog"
   - Use retrieve() with method="interference"
   - Capture metrics using helpers from conftest
   - Evaluate all three success criteria
   - Print metrics JSON
   - Assert: At minimum, relaxed (top-3) criterion should pass
   - If target not in top-3, document as LIMITATION

2. **test_success_criteria_hierarchy** (parametrized by noise_level):
   - Create noisy memory with single target pattern
   - Retrieve and evaluate all three criteria
   - Capture: which criteria passed/failed at each noise level
   - Print comparison showing strict <= relaxed (logically)
   - Document which criteria hold under which noise levels

3. **test_near_miss_degradation** (parametrized by noise_level AND near_miss_count [1, 3, 5]):
   - Use custom NoiseConfig with specified near_miss_ratio
   - Keep clutter_ratio=3.0 constant
   - Retrieve target and capture metrics
   - Track target_rank across near_miss_counts to observe degradation curve
   - Print degradation metrics for Phase 8 analysis
   - Assert: Target should at least be found (in results), even if not top-1

4. **test_precision_vs_recall** (parametrized by noise_level):
   - Create noisy memory with 3 target patterns
   - For each target, retrieve and capture:
     - Recall: Is target in results? (boolean)
     - Precision penalty: How many near-misses in top-K? (count/total)
   - Aggregate precision/recall across targets
   - Print aggregated metrics
   - Assert: Recall > 0.5 (at least half of targets found in top results)

5. **test_relative_ranking_robustness** (parametrized by noise_level):
   - Focus specifically on relative criterion (target above near-misses)
   - Create noisy memory with single target
   - Retrieve and get ranks of target AND all its near-misses
   - Calculate: What percentage of near-misses rank below target?
   - Print relative ranking metrics
   - Document if near-misses consistently beat target (indicates retrieval needs improvement)

Use @pytest.mark.stress and @pytest.mark.parametrize for all tests.

IMPORTANT: Near-misses are designed to be challenging. Some tests may show limitations - document these clearly for Phase 8 analysis rather than forcing assertions that will flake.
  </action>
  <verify>
```bash
cd C:\Users\jinwi\programming_files_NEW\engram && python -m pytest tests/stress/test_interference.py -v --tb=short 2>&1 | head -60
```
  </verify>
  <done>TEST-02 complete: test_interference.py has 5 test methods covering basic retrieval, success criteria hierarchy, near-miss degradation, precision/recall, and relative ranking</done>
</task>

<task type="auto">
  <name>Task 2: Verify TEST-02 coverage and metrics output</name>
  <files>tests/stress/test_interference.py</files>
  <action>
Run all interference retrieval tests and verify:

1. All tests pass or document limitations appropriately
2. Metrics are printed to stdout (JSON format)
3. Both MEDIUM and HIGH noise levels are tested
4. Near-miss degradation shows meaningful variation

Run: `python -m pytest tests/stress/test_interference.py -v -s`

Capture the output and verify:
- At least 12 test cases run (base tests x noise levels x near_miss_counts)
- Metrics appear in output with all fields populated
- No unexpected failures

If any test shows consistent issues:
- Check that retrieve() with method="interference" is working correctly
- Verify noisy_memory fixture is creating appropriate near-misses
- Check that near-miss overlap (0.4) is low enough to differentiate

Document any interesting patterns in test docstrings:
- Does degradation appear linear or cliff-like?
- Does relative criterion hold better than strict?
- Are there differences between MEDIUM and HIGH noise?
  </action>
  <verify>
```bash
cd C:\Users\jinwi\programming_files_NEW\engram && python -m pytest tests/stress/test_interference.py -v -s 2>&1 | tail -40
```
  </verify>
  <done>All TEST-02 interference tests pass with metrics output, degradation behavior documented</done>
</task>

</tasks>

<verification>
- [ ] tests/stress/test_interference.py exists and imports from stress conftest
- [ ] TestInterferenceRetrieval class implements all 5 test methods
- [ ] All tests parametrized by noise_level (MEDIUM, HIGH)
- [ ] Near-miss degradation test parametrized by near_miss_count (1, 3, 5)
- [ ] Metrics printed for every test run (JSON format)
- [ ] All three success criteria evaluated and reported
- [ ] Precision and recall both measured
- [ ] Tests pass or skip with documented limitations
- [ ] Existing tests still pass: `pytest tests/ --ignore=tests/stress -q`
</verification>

<success_criteria>
- TEST-02 requirement satisfied: Interference retrieval tests with noisy memory validate finding relevant among noise
- All three success criteria (strict, relaxed, relative) empirically tested
- Near-miss count parametrization enables Phase 8 degradation curve analysis
- Both precision and recall validated
- Metrics output enables Phase 8 comparison and analysis
</success_criteria>

<output>
After completion, create `.planning/phases/07-harder-behavior-tests/07-02-SUMMARY.md`
</output>
